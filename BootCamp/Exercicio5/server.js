// Tambem nativo do Sistema (Traz informacoes da sistema)
const os = require('os');

// Oque ta rodando no CPU?
// console.log(os.cpus());

// Total me memoria do sistema
// console.log(os.totalmem());

// Versao do sistema
console.log(os.type());

